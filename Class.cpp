/* 클래스란 객체지향에서 객체의 특징을 담아놓는 설계도와 같다. 

클래스의 구조 - 

Field(필드) 클래스에 포함되어있는 변수
ㄴInstance variable(인스턴트변수) 인스턴스가 생성될 때 생성되는 변수, 서로 독립적인 값을 가지고 있다.
ㄴStatic variable(클래스변수) 인스턴스 변수 앞에 static 키워드가 붙여진 변수를 의미한다. 해당 클래스에서 파생된 인스턴스는 클래스 변수를 공유하며, 인스턴스가 없어도 사용 가능하다.
ㄴLocal variable(지역변수) 해당 메서드 안에서만 사용하는 변수

Method(메서드) 객체의 특징. public void asdf() { 이런데에 들어가는 게 메서드이다.}




*/

#include <iostream>

using namespace std; //namespace 에 있는 클래스(std)에 정의된 함수들을 사용하겠다는 의미

class //여기서부터 졸려서 중간저장
